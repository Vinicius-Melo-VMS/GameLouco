using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class destroirOBJ : MonoBehaviour
{

    public GameObject ttt;
    // Start is called before the first frame update
    void Start()
    {
        Destroy(ttt);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
